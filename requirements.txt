# Core UI and Backend
streamlit>=1.28.0
fastapi>=0.110.0
uvicorn[standard]>=0.23.0
Flask==2.3.3
Werkzeug==2.3.7

# Data Science and ML
scikit-learn>=1.3.0
pandas==2.0.3
numpy==1.24.3

# HTTP and API
requests==2.31.0

# Note: Built-in modules (json, urllib, re, random, time, datetime, 
# collections, hashlib, os, pathlib, threading, typing) do not need 
# to be listed in requirements.txt
